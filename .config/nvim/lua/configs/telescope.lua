return {
    defaults  = {
        mappings = {
            n = { ["<A-d>"] = require("telescope.actions").close },
            i = { ["<A-d>"] = require("telescope.actions").close }
        }
    }
}

